# Word Group App

Built using Vite + React, this app fetches a group of words that fit a description inputted by a user using the OpenAI GPT API. Users can then create rules to highlight and tag words.
